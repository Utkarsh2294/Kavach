# FastAPI + async stack
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Database
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.9

# Redis
redis[hiredis]>=5.0.0

# Auth (Phase 07 will use these)
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0

# Model serving — the committed artifacts contain scikit-learn and XGBoost
# estimators, so these must be present in the production image as well.
numpy==2.4.1
scikit-learn==1.9.0
# The CPU distribution provides the same `xgboost` import without bundling
# NVIDIA/NCCL packages into an API container that performs CPU inference.
xgboost-cpu==3.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
